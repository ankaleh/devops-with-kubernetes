apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kub-project-ing
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: kub-project-svc
            port:
              number: 3456